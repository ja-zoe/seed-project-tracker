# R21.15 — Recent Standings omitted Next Week's Goals

**Status:** done (merged in `1814669`)
**Files:** `src/app/(app)/projects/[id]/page.tsx`

## Spec

**Problem:** The project detail page's "Recent Project Standings" summary omitted the required "Next Week's
Goals" field that the full history page renders — the recent-N summary and the see-all page had diverged on
which fields of the same StatusUpdate record they show.

**Approach:** Add a "Next Week" block rendering `nextWeekGoals` to the Recent Project Standings grid,
matching the history page. The page's query uses Prisma `include` with no scalar `select` narrowing, so the
data was already fetched — purely a missing JSX element.

## Tests

Retroactive — verified during the run: typecheck + the field renders on the project page. No dedicated
Playwright spec.

## Notes / log
- 2026-07-01 — Shipped as gnhf iteration 16 (`1814669`). Learning: cross-surface parity includes
  display-field parity, not just edit/delete controls. Companion memory: `standing-nextweek-display-gap`.

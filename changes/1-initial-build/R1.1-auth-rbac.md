# R1.1 — Auth + RBAC (Phase 1)

**Status:** done
**Files:** `src/auth.ts`, `src/auth.config.ts`, middleware, seed script, Prisma schema

## Spec

Auth.js v5 Credentials provider with CAS (mock mode). Two-layer RBAC: global Role permissions + per-project assignment role. Seed built-in roles and the Settings singleton.

## Notes / log

- 2026-06-26 — Complete (`feat/phase-1-auth-rbac`). Auth.js v5 Credentials provider with HMAC handoff tokens (60s TTL). CAS mock mode via `/dev-login` server action; real CAS callback skeleton in place. Prisma v7 client uses `@prisma/adapter-pg` (WASM engine requires driver adapter). Schema enum names aligned with existing DB (TimelineStatus, Channel, RecipientGroup). 3 built-in roles + Settings singleton seeded. PM auto-activates on first sign-in. Edge-safe middleware: `auth.config.ts` (no Node) + full `auth.ts`. DB reset done via the 6543 pooler.

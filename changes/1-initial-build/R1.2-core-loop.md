# R1.2 — Core loop (Phase 2)

**Status:** done
**Files:** `src/app/(app)/` route group, `src/lib/actions/projects.ts`, `deliverables.ts`, status/meeting forms

## Spec

The core PM workflow: projects CRUD, deliverables + subtasks, the pre-meeting status form, and the post-meeting record that updates project status.

## Notes / log

- 2026-06-26 — Complete (`feat/phase-2-core-loop`). `(app)` route group with sidebar layout gated by permissions. Projects CRUD; Deliverables + Subtasks with Markdown write/preview (react-markdown + remark-gfm). Pre-meeting status form with late-marking (reads `Settings.submissionDeadlineHours`). Post-meeting meeting-record form; updates `project.status` unless `statusOverride`. Member management (LEAD/SUBLEAD/MEMBER). My Tasks page for assigned subtasks.

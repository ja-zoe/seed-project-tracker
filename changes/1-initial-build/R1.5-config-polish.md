# R1.5 — Configurability & polish (Phase 5)

**Status:** done
**Files:** `/pm/users`, role builder, `/pm/settings`, `src/lib/notifications.ts`, `/api/cron/notifications`, sidebar

## Spec

PM configurability (user management, role builder, detection thresholds, notification rule CRUD), the notification engine + cron trigger, and responsive/motion polish.

## Notes / log

- 2026-06-26 — Complete (`feat/phase-5-polish`). `/pm/users`: approve/suspend/reactivate users, assign roles. Role builder: edit permissions on all roles, create/delete custom roles. `/pm/settings`: detection thresholds + notification rule CRUD. Notification engine fires MISSING_SUBMISSION / PROJECT_BEHIND / ACTION_ITEM_DUE / GOAL_MISSED rules via `/api/cron/notifications` (CRON_SECRET). In-app bell in sidebar footer with unread badge + dropdown, auto-polls 60s. ScrollReveal GSAP wrapper with prefers-reduced-motion guard. Mobile-responsive sidebar with hamburger drawer.

# R20.2 — Date-input robustness (invalid dates must not crash the page)

**Status:** tests passing — shared-guard approach confirmed by user 2026-06-30.
**Files:** the date-accepting server actions (`src/lib/actions/action-items.ts`, `deliverables.ts`,
`projects.ts`, `status-updates.ts`), a new shared helper (e.g. `src/lib/date.ts` `parseDateInput`), and the
date `<input type="date">` sites: `project-modal.tsx`, `deliverable-modal.tsx`, `subtask-modal.tsx`,
`action-item-modal.tsx`, `action-items-section.tsx` (inline deadline), `sortable-deliverables.tsx` (inline
dates), `projects/[id]/deliverables/new/page.tsx`, `.../subtasks/[sid]/edit/page.tsx`.

## Spec

**Problem:** Entering an impossible date such as **06/31/2026** crashes the **whole page** (runtime error)
instead of being caught in the date field. Dates are entered via raw native `<input type="date">` and the
server actions parse them with unguarded `new Date(raw)` (e.g. `action-items.ts`:
`const deadline = deadlineRaw ? new Date(deadlineRaw) : null`). An invalid/out-of-range value yields an
`Invalid Date`, which then throws when Prisma tries to persist it (or when a later `.toISOString()` /
`toLocaleDateString` runs on it), bubbling up as an unhandled error that takes down the page.

**Approach:** Make invalid dates a *handled, field-level* condition end to end.

1. **Root-cause first.** Reproduce 06/31/2026 in a date field (e.g. an action-item deadline or subtask due
   date) and capture the exact throw site (parse vs persist vs render). Fix that path specifically, then
   apply the general guards below so no date path can crash.

2. **Shared safe parse (server).** Add `src/lib/date.ts`:
   ```ts
   /** Returns a valid Date, null for empty, or throws a typed ValidationError for an invalid value. */
   export function parseDateInput(raw: FormDataEntryValue | null): Date | null
   ```
   It trims, returns `null` for empty, builds the Date, and treats `isNaN(d.getTime())` (and impossible
   calendar dates) as invalid. Replace every unguarded `new Date(raw)` in the date-accepting actions with
   this. On invalid, the action must **not throw an unhandled error** — return a structured error the caller
   can show (or no-op the field), so a bad value never reaches Prisma or `.toISOString()`.

3. **Field-level validation (client).** The user must see the error "in the date picker." Add inline
   validation to the date inputs: on change/blur, if the value is an impossible date, show a small inline
   error message near the field (Forest Floor clay `#A4503C`) and block submit, rather than posting it.
   Because there are ~8 native date inputs, prefer a small shared client wrapper (e.g. `DateField`) that
   renders `<input type="date">` + an inline error slot and runs the same validity check, so the behavior is
   consistent everywhere. (If a full wrapper is too invasive for this fix, at minimum guard the submit
   handlers in the modals/inline editors to validate before calling the server action.)

4. **Render guards.** Anywhere a stored date is formatted for display
   (`new Date(iso).toLocaleDateString(...)` / `.toISOString()` in `sortable-deliverables.tsx`,
   `action-items-section.tsx`), tolerate a bad/missing value (guard `isNaN`) so display never throws.

**State:** validation state is local to each date field/modal (client); the server helper is pure. **Data
flow:** input change → client validity check → (valid) server action → `parseDateInput` → Prisma; (invalid)
inline error, no submit.

**Non-obvious constraints:** native `<input type="date">` behavior varies by browser/locale — don't rely on
it to reject impossible dates; validate explicitly. Keep the existing `timeZone: "UTC"` formatting (the app
deliberately formats dates in UTC — see R14 date-UTC work) so this fix doesn't shift displayed dates.

## Tests

- [x] `pnpm build` clean.
- [x] **Repro fixed / impossible dates rejected:** 06/31 and 02/30 are rejected by the shared guard
      (`isValidDateInput` false; `parseDateInput` throws `DateInputError`) so they can't silently roll over
      to 07/01 / 03/02 or reach Prisma as an Invalid Date. *(`e2e/r20-date-robustness.spec.ts` helper tests.)*
      Note: native `<input type="date">` won't emit an impossible value through the picker, so the guard is
      the defensive backstop; client `isValidDateInput` also blocks submit in every modal/inline editor.
- [x] A valid date still saves correctly and displays in UTC (no off-by-one shift). *(UI test — a deliverable
      with target 2026-12-31 renders "Target: Dec 31, 2026", not Dec 30.)*
- [x] Server guard: every date-accepting action (`projects`, `deliverables`, `action-items`,
      `meeting-records`, `calendar`) uses `parseDateInput`, which returns null/Date or throws a typed
      `DateInputError` — no `Invalid Date` is ever written. Modals/inline editors wrap the action call in
      try/catch so a thrown error surfaces inline instead of crashing the page. *(UI test — an invalid range
      shows an inline error, modal stays open, no Next error overlay.)*
- [x] Spot-checked each date input site: project-modal, deliverable-modal, subtask-modal, action-item-modal,
      action-items-section (inline deadline), sortable-deliverables (inline deliverable dates + subtask due)
      — all validate client-side + catch; render guards added to `formatDateOnly` (utils) and the local
      `formatDate` (sortable-deliverables). *(r9-deliv-modal, r8-subtask-modal, r13-action-item-modal,
      r13-action-item-inline, r8-date-bounds all green.)*

## Notes / log
- 2026-06-30 — Implemented on `feat/set20/R20.2-date-input-robustness`. Added `src/lib/date.ts`:
  `parseDateInput` (empty→null, valid→Date anchored to UTC midnight for date-only, invalid/impossible→throw
  `DateInputError`) + `isValidDateInput` (pure client/server check; rejects rollover-impossible dates by
  round-tripping the `yyyy-mm-dd` UTC components). Key finding: `new Date("2026-06-31")` does NOT yield an
  Invalid Date — it silently rolls over to 07-01, so the guard verifies component round-trip rather than just
  `isNaN`. Replaced every unguarded `new Date(raw)` on user input in `projects.ts`, `deliverables.ts`,
  `action-items.ts`, `meeting-records.ts`, `calendar.ts` (left `new Date()`/DB-value parses alone). Client:
  `isValidDateInput` checks + try/catch around the action call in deliverable/project/subtask/action-item
  modals and the action-items inline deadline + sortable-deliverables inline deliverable-dates/subtask-due
  editors, so a bad value or a server throw surfaces inline (or no-ops the field) instead of crashing.
  Render guards added to `formatDateOnly` and the local `formatDate` (return "—" for an unparseable value).
  `pnpm build` clean; `e2e/r20-date-robustness.spec.ts` green (4 tests). Regression: r9-deliv-modal,
  r8-subtask-modal, r13-action-item-modal, r13-action-item-inline, r8-date-bounds all green.
  - **Datetime-local edge (out of scope):** calendar uses native datetime-local; an impossible time string
    there still rolls over (only `isNaN` is checked for non-date-only values). Native datetime-local
    constrains input through the picker, and the calendar wasn't part of the reported bug, so left as-is.
  - **Pre-existing unrelated failure:** `e2e/r9-project-modal.spec.ts` fails at `.fill()` on the
    `project-modal-semester` SemesterField (it renders a `<select>` when prior semesters exist — same
    semester-selector brittleness already noted in set 19). The modal opens and the name field fills fine;
    the failure is before any date logic. Not caused by this work.

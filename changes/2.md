# Revision Set 2 — Bootstrap & Scaffolding

This file is the canonical bootstrap prompt and running log for Revision Set 2.
Load it at the start of any session continuing this work.

---

## Project context

**App:** SEED Project Tracker — Next.js 16 (App Router, React 19, TypeScript strict)
**Repo root:** `/home/jazoe/projects/seed-project-dashboard`
**Primary branch strategy:** `develop` = always-working integration; features on `feat/rev2-*` off develop; never merge non-booting code.
**Package manager:** pnpm only. Never npm/npx.
**DB:** Supabase Postgres via pgBouncer pooler (port 6543). `prisma db push` / direct port 5432 = unreachable. Apply DDL via `scripts/apply-schema.ts` (pg Client directly).
**Prisma v7:** `new PrismaClient({ adapter })` where `adapter = new PrismaPg({ connectionString: DATABASE_URL })`. WASM engine requires driver adapter — never construct without it.
**Auth:** Auth.js v5 Credentials + CAS mock (`CAS_MODE=mock`). PM auto-activates via `PM_ADMIN_EMAIL`.
**Icons:** Phosphor Icons only (Bold/Fill weight). Lucide is banned.
**Styles:** Tailwind v4, `@import "tailwindcss"`, `@theme inline`. Forest Floor palette (`#F4F1EA` canvas, `#2E4034` primary, `#588157` moss). No heavy shadows, no gradients, no glassmorphism.

---

## Revision Set 2 — Feature list

### Status
- [x] R2.1 — Fix /pm/users crash (onClick event handler in Server Component → remove dead button)
- [x] R2.2 — Prevent double-submit on subtask/action-item/deliverable create forms (`SubmitButton` using `useFormStatus`)
- [x] R2.3 — Edit subtask page (`/projects/[id]/deliverables/[did]/subtasks/[sid]/edit`)
- [x] R2.4 — Edit action item page (`/projects/[id]/action-items/[aid]/edit`)
- [x] R2.5 — Deliverable groups — optional `group` label on each deliverable; visually separate groups in project detail and timeline views
- [x] R2.6 — Sort deliverables by status (client-side toggle on project detail)

---

## Conventions (same as Rev 1)

- Branches: `develop` = always-working integration branch; `feat/rev2-*` for revision 2 work.
- Promote `develop` → `main` only when told.
- Never merge non-booting code into `develop`.
- DB schema additions go through `scripts/apply-schema.ts`.
- Log all completed work below under `## Log`, newest entry on top.

---

## DB changes in this revision

### Deliverable.group (nullable String)
Applied via `scripts/apply-schema.ts`:
```sql
ALTER TABLE "Deliverable" ADD COLUMN IF NOT EXISTS "group" TEXT;
```
`prisma/schema.prisma` updated: `group String?` field on `Deliverable`.

---

## Log

- 2026-06-26 — Rev 2 complete. `feat/rev2-fixes-and-groups` merged to `develop`.
  Crash fix: removed dead onClick button from /pm/users Server Component.
  SubmitButton component (useFormStatus) guards all create/edit forms.
  Edit subtask: /projects/[id]/deliverables/[did]/subtasks/[sid]/edit with updateSubtask action.
  Edit action item: /projects/[id]/action-items/[aid]/edit with updateActionItem action.
  Deliverable groups: nullable `group` field on Deliverable; project detail shows
  grouped sections with group header; create/edit forms include group input.
  Sort deliverables: SortableDeliverables client component — toggle between
  default (orderIndex) and status-sorted views on project detail.

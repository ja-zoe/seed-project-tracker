# R4.0 — Fix R3.4 carry-over bugs

**Status:** done
**Files:** `package.json`, `src/app/api/ai/verify/route.ts` (later removed)

## Spec

- **R4.0a:** `pnpm add zod` — fixes build-blocking `Module not found: 'zod'` in `/api/chat`.
- **R4.0b:** `POST /api/ai/verify` — verify an API key before accepting it (spinner + error surface).

## Notes / log

- 2026-06-27 — R4.0a done (zod installed). R4.0b built and confirmed working, then **superseded by the MCP pivot** — the endpoint and the chat widget it served were removed in R4.1.

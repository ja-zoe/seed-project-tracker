# R19.2 — Site-wide rollout + audit (with Playwright verification)

**Status:** done — merged to main (local, not pushed) 2026-06-30. Convention applied site-wide; `pnpm build` clean; Playwright
`e2e/r19-affordances.spec.ts` green. Open Question 2 resolved per the recommendation (interactive elements
only; static display elements left untouched).
**Files:** broad — components under `src/components/` and interactive elements across `src/app/(app)/**`.
See the audit matrix below; treat it as the working checklist (update it as the audit proceeds).

## Spec

**Problem:** Interactivity is signalled inconsistently across the app. R19.1 defines the cue; this feature
**applies it everywhere** so that on hover every clickable element gives a clear visual cue, clickable icons
always switch to a pointer cursor + color, and similarly-styled interactive objects behave identically
(deliverables, inline-editable items, subtasks, etc.).

**Approach:** Audit the entire site for interactive elements and apply the R19.1 classes consistently,
replacing ad-hoc `hover:` treatments where they diverge. Apply the **row-vs-control distinction**:
container rows that open get `.clickable-row` (subtle tint, **default cursor**); the controls inside and all
other clickable icons/links/buttons get `.clickable-icon` / `.clickable` (pointer + color), destructive →
`.clickable-danger`. Static/display elements are left alone (Open Question 2). Work component-by-component
using the matrix below.

**Audit matrix (verify + fix each; check off during implementation):**
- [x] `components/sortable-deliverables.tsx` — deliverable header + subtask row body → `.clickable-row`
      (tint, default cursor); title/dates/desc pencils, status/priority/group chips, move up/down, edit/
      delete, assignee, add-subtask, sort toggle, portal-menu items → `.clickable-icon`/`cursor-pointer`;
      delete triggers → `.clickable-danger`; `InlineConfirm` ✓/✗ + slide-panel save/cancel/yes-no got
      `cursor-pointer` (semantic green/clay colors preserved).
- [x] `components/action-items-section.tsx` — edit pencil → `.clickable-icon`, reopen → `.clickable-icon`,
      "new"/"mark done" buttons → `cursor-pointer`; inline-edit text fields kept `cursor-text` (deliberate
      "editable text" affordance — see judgment note below).
- [x] `components/action-item-modal.tsx` — footer buttons via shadcn `Button` (now `cursor-pointer` in base).
- [x] `components/meeting-record-controls.tsx` — delete trigger → `.clickable-danger`.
- [x] `components/status-update-controls.tsx` — edit → `.clickable-icon`, delete → `.clickable-danger`.
- [x] `components/project-modal.tsx`, `components/projects-list.tsx` — project Link cards already pointer;
      select/cancel → `.clickable-icon`, selectable card + bulk-delete buttons → `cursor-pointer`,
      remove/danger → `.clickable-danger`.
- [x] `components/semester-calendar.tsx` — day cells already pointer+tint; view toggles + add-event +
      delete got `cursor-pointer`/`.clickable-danger`.
- [x] `app/(app)/account/mcp-token-section.tsx` — copy → `.clickable-icon`, revoke → `.clickable-danger`,
      generate/regenerate → `cursor-pointer`.
- [x] `app/(app)/pm/users/page.tsx` — approve/invite/save buttons → `cursor-pointer`, delete/danger →
      `.clickable-danger`/`cursor-pointer`, role labels already pointer.
- [x] App shell sidebar/nav (`components/sidebar.tsx`) — nav links already pointer (Link) + bg/color hover;
      sign-out button → `cursor-pointer`. notification-bell button → `cursor-pointer`.
- [x] Swept the rest: shadcn `ui/button.tsx` base gained `cursor-pointer` (covers every `<Button>` across
      modals/forms/pages); pervasive raw primary (`bg-primary text-primary-foreground`) and secondary
      (`border border-border bg-card`) submit buttons + check-circle done buttons + markdown-editor tabs +
      status-submit-switcher + pm/settings rule toggles all gained `cursor-pointer`.

**State:** none (presentational only). **Constraints:** no behavior changes — only affordances; preserve
existing focus rings, `data-testid`s, and `aria` attributes; keep within Forest Floor tokens; no new deps.

## Tests

Playwright-driven (the request requires it). Add/extend an e2e under the existing test setup; drive the
deployed-or-local app and assert the affordance on representative elements of each audited surface:

- [x] `pnpm build` clean.
- [x] **Pointer cursor (controls):** account generate/regenerate button, deliverable title pencil, and
      deliverable delete all assert `cursor === "pointer"`.
- [x] **Default cursor (rows):** deliverable header + every subtask row body assert `cursor !== "pointer"`,
      while the title pencil / `pencil-btn` inside assert `cursor === "pointer"` — confirms the split.
- [x] **Row hover tint:** captured `background-color` at rest vs. hover on the deliverable header and a
      subtask row; both change on hover.
- [x] **Hover color cue (controls):** deliverable title pencil hover color = forest `rgb(46,64,52)`;
      deliverable delete hover color = clay `rgb(164,80,60)`.
- [x] **Consistency:** asserted across *all* subtask row bodies (cursor not pointer) and *all* `pencil-btn`
      inline-edit triggers (cursor pointer) — multiple instances, not one.
- [x] **No false affordance:** `[data-testid="project-status-badge"]` (a non-button `<span>`) asserts
      `cursor !== "pointer"` and unchanged `color` on hover.
- [x] Screenshots `e2e/screenshots/r19-deliverables.png` + `r19-action-items.png` captured for the reviewer.

## Notes / log
- 2026-06-30 — Implemented the rollout. Mechanical conversions (perl) for the two dominant idioms
  (`text-muted-foreground hover:text-foreground transition-colors` → `clickable-icon`; muted→clay danger →
  `clickable-danger`) plus per-component edits for rows, chips, confirm micro-buttons, and the pervasive
  primary/secondary submit-button idiom. Added `cursor-pointer` to the shadcn `Button` base so every
  `<Button>` is covered in one place. Added `data-testid="project-status-badge"` to `ProjectStatusBadge`
  (additive; needed a reliable standalone static element for the no-false-affordance assertion).
- **Hover color = forest `var(--primary)` (#2E4034), not `--foreground` (#2B2B26)** — per the user's Open
  Question 1 wording ("Forest Floor tokens, normal → forest #2E4034"). R19.1's class CSS uses `--primary`.
- **Judgment calls (interactive vs static):**
  - Action-item inline-edit text fields (description/owner/deadline) kept `cursor-text` + their existing
    hover color shift rather than `pointer` — they are click-to-edit *text*, and the text caret is the
    clearer signal; they already carry a hover cue, so this stays inside the convention's spirit.
  - Calendar day cells and notification rows that *navigate/act* on click keep `cursor-pointer` (+ existing
    bg tint) rather than `.clickable-row` — they behave like buttons/links, not "rows you read".
  - Expandable `<summary>` rows (pm/users, action-items) kept their native pointer + bg hover (they are
    disclosure controls, not the deliverable/subtask reading rows the user's row-rule targets).
  - Status badges (`status-badge.tsx`) and plain meta text left with no cue (static display).
- **Parallel-safety / e2e note:** ran on port 3100 with marker `[e2e19]` (temp tweaks reverted). The shared
  dev DB had been migrated by the parallel Set 18 agent to include a new `DELETE_USERS` Permission enum
  value (granted to the Project Manager role), which main's Prisma client could not deserialize (P2023).
  To run e2e I temporarily added `DELETE_USERS` to `prisma/schema.prisma` + regenerated, then reverted it
  and regenerated back — it is **not** part of this set's committed changes. Also copied the (gitignored)
  `.env` into the worktree so the dev server had DB/auth secrets.

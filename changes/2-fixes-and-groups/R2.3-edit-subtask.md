# R2.3 — Edit subtask page

**Status:** done
**Files:** `src/app/(app)/projects/[id]/deliverables/[did]/subtasks/[sid]/edit/page.tsx`, `src/lib/actions/deliverables.ts`

## Spec

Add an edit page for subtasks at `/projects/[id]/deliverables/[did]/subtasks/[sid]/edit` with an `updateSubtask` server action.

## Notes / log

- 2026-06-26 — Edit subtask page + `updateSubtask` action shipped.

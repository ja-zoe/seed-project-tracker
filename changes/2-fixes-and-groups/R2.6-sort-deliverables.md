# R2.6 — Sort deliverables by status

**Status:** done
**Files:** `src/components/sortable-deliverables.tsx`

## Spec

Add a client-side toggle on the project detail page to switch between default order (`orderIndex`) and a status-sorted view.

## Notes / log

- 2026-06-26 — `SortableDeliverables` client component toggles between default (orderIndex) and status-sorted views.

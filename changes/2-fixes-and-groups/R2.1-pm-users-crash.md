# R2.1 — Fix /pm/users crash

**Status:** done
**Files:** `src/app/(app)/pm/users/page.tsx`

## Spec

**Problem:** `/pm/users` crashed — an `onClick` event handler was attached to a button inside a Server Component.

**Approach:** Remove the dead button (it had no working handler in the server context).

## Notes / log

- 2026-06-26 — Removed the dead onClick button from the `/pm/users` Server Component.

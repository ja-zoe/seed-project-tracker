import { PageLoader } from "@/components/PageLoader";

// Generic in-app navigation fallback so route changes show immediate feedback.
export default function AppLoading() {
  return <PageLoader />;
}
